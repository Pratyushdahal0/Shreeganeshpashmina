import { db } from "@/lib/prisma";
import Link from "next/link";
export const dynamic = 'force-dynamic';
export default async function AdminPage() { const prisma = db(); const week = new Date(Date.now() - 7 * 86400000); const [enquiries, products, orders, pendingOrders] = prisma ? await Promise.all([prisma.enquiry.count({ where: { createdAt: { gte: week }, status: 'NEW' } }), prisma.product.count({ where: { status: 'ACTIVE' } }), prisma.order.count({ where: { createdAt: { gte: week } } }), prisma.order.count({ where: { status: { in: ['PENDING', 'PAYMENT_PENDING', 'PAID', 'PROCESSING'] } } })]) : [0, 0, 0, 0]; return <section className="adminDashboard"><header className="dashboardHeader"><h1>Dashboard</h1><p>Catalogue, orders and enquiries — everything at a glance.</p></header><div className="dashboardMetrics"><article><span className="metricIcon">◉</span><p>Orders this week</p><strong>{orders}</strong><small>{pendingOrders} awaiting processing</small></article><article><span className="metricIcon">▢</span><p>New enquiries</p><strong>{enquiries}</strong><small>Received in the last 7 days</small></article><article><span className="metricIcon">◌</span><p>Live products</p><strong>{products}</strong><small>Available in the online catalogue</small></article><article><span className="metricIcon">⌁</span><p>Store status</p><strong className="metricGood">Live</strong><small>Storefront is available to visitors</small></article></div><div className="dashboardLower"><article className="dashboardPanel activityPanel"><div className="panelHeading"><h2>Recent activity</h2><Link href="/admin/orders">Manage ↗</Link></div><div className="activityEmpty"><span>⌁</span><p>New orders and enquiries will appear here as they are created.</p></div></article><article className="dashboardPanel healthPanel"><div className="panelHeading"><h2>Store health</h2><span>4/4</span></div><p className="healthLead">Essentials configured</p><ul><li>✓ Store title set</li><li>✓ Product catalogue active</li><li>✓ Enquiry messages enabled</li><li>✓ Editorial content configured</li></ul></article></div>{!prisma && <p className="formError">DATABASE_URL is not configured. Add it in Vercel before managing live content.</p>}</section>; }
