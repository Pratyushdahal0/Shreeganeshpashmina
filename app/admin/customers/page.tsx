import { db } from "@/lib/prisma";
export const dynamic = "force-dynamic";
export default async function CustomersPage() { const prisma = db(); const customers = prisma ? await prisma.customer.findMany({ include: { _count: { select: { orders: true } }, orders: { select: { totalMinor: true } } }, orderBy: { createdAt: "desc" }, take: 100 }) : []; return <><div className="sectionHead"><div><div className="eyebrow">Commerce</div><h2>Customers</h2></div></div>{customers.length ? <table className="adminTable"><thead><tr><th>Customer</th><th>Contact</th><th>Country</th><th>Orders</th><th>Total spent</th></tr></thead><tbody>{customers.map((customer) => { const spend = customer.orders.reduce((sum, order) => sum + order.totalMinor, 0); return <tr key={customer.id}><td>{customer.name}</td><td>{customer.email || customer.phone || "—"}</td><td>{customer.country || "—"}</td><td>{customer._count.orders}</td><td>NPR {(spend / 100).toLocaleString()}</td></tr>; })}</tbody></table> : <div className="adminEmpty"><h3>No customers yet</h3><p>Customers are created from real orders and can later be enriched with notes, addresses and tags.</p></div>}</>; }
