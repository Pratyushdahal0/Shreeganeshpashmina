import { db } from "@/lib/prisma";
import InventoryAdjuster from "@/components/InventoryAdjuster";
export const dynamic = "force-dynamic";
export default async function InventoryPage() { const prisma = db(); const rows = prisma ? await prisma.inventoryItem.findMany({ include: { variant: { include: { product: true } } }, orderBy: { updatedAt: "desc" } }) : []; return <><div className="sectionHead"><div><div className="eyebrow">Commerce</div><h2>Inventory</h2></div></div>{rows.length ? <table className="adminTable"><thead><tr><th>Product</th><th>Variant</th><th>SKU</th><th>On hand</th><th>Reserved</th><th>Available</th><th /></tr></thead><tbody>{rows.map((row) => <tr key={row.id}><td>{row.variant.product.name}</td><td>{row.variant.title}</td><td>{row.variant.sku}</td><td>{row.onHand}</td><td>{row.reserved}</td><td>{row.onHand - row.reserved}</td><td><InventoryAdjuster inventoryId={row.id} /></td></tr>)}</tbody></table> : <div className="adminEmpty"><h3>Inventory starts with variants</h3><p>Add product variants and their opening stock to begin an auditable inventory history.</p></div>}</>; }
