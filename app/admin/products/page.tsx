import Link from "next/link"; import { catalogue } from "@/lib/catalogue"; import CreateProductForm from "@/components/CreateProductForm";
export const dynamic = 'force-dynamic';
export default async function ProductsAdmin() { const products = await catalogue(); return <><div className="sectionHead"><div><div className="eyebrow">Catalogue</div><h2>Products & SEO</h2></div><CreateProductForm /></div><table className="adminTable"><thead><tr><th>Product</th><th>Slug</th><th>SEO title</th><th /></tr></thead><tbody>{products.map((product) => <tr key={product.id}><td>{product.name}</td><td>{product.slug}</td><td>{product.metaTitle || 'Generated from product details'}</td><td><Link className="btn" href={`/admin/products/${product.id}`}>Edit</Link></td></tr>)}</tbody></table></>; }
