import { db } from "@/lib/prisma";
export const dynamic = "force-dynamic";
export default async function AuditLogsPage() { const prisma = db(); const logs = prisma ? await prisma.auditLog.findMany({ include: { user: true }, orderBy: { createdAt: "desc" }, take: 200 }) : []; return <><div className="sectionHead"><div><div className="eyebrow">System</div><h2>Audit logs</h2></div></div>{logs.length ? <table className="adminTable"><thead><tr><th>When</th><th>Action</th><th>Entity</th><th>Staff</th></tr></thead><tbody>{logs.map((log) => <tr key={log.id}><td>{log.createdAt.toLocaleString()}</td><td className="status">{log.action.replaceAll("_", " ")}</td><td>{log.entityType} · {log.entityId}</td><td>{log.user?.name || "System"}</td></tr>)}</tbody></table> : <div className="adminEmpty"><h3>No recorded actions yet</h3><p>Administrative changes are recorded here as staff begin using the system.</p></div>}</>; }
