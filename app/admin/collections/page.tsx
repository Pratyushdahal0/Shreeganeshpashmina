import { db } from "@/lib/prisma";
export const dynamic = "force-dynamic";
export default async function CollectionsPage() { const prisma = db(); const collections = prisma ? await prisma.collection.findMany({ include: { _count: { select: { products: true } } }, orderBy: { name: "asc" } }) : []; return <><div className="sectionHead"><div><div className="eyebrow">Commerce</div><h2>Collections</h2></div></div>{collections.length ? <table className="adminTable"><thead><tr><th>Collection</th><th>Products</th><th>Status</th><th>Updated</th></tr></thead><tbody>{collections.map((item) => <tr key={item.id}><td>{item.name}</td><td>{item._count.products}</td><td className="status">{item.isActive ? "Active" : "Inactive"}</td><td>{item.updatedAt.toLocaleDateString()}</td></tr>)}</tbody></table> : <div className="adminEmpty"><h3>No collections yet</h3><p>Collections are ready for real merchandising groups such as shawls, stoles or knitwear.</p></div>}</>; }
