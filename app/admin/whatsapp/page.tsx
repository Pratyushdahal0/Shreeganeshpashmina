import { db } from "@/lib/prisma";
import WhatsAppInquiryStatus from "@/components/WhatsAppInquiryStatus";
import WhatsAppConvertOrder from "@/components/WhatsAppConvertOrder";
export const dynamic = "force-dynamic";
export default async function WhatsAppPage() { const prisma = db(); const [inquiries, variants] = prisma ? await Promise.all([prisma.whatsAppInquiry.findMany({ include: { product: true, convertedOrder: true }, orderBy: { createdAt: "desc" }, take: 100 }), prisma.productVariant.findMany({ where: { isActive: true }, include: { product: true, inventory: true } })]) : [[], []]; const options = variants.map((variant) => ({ id: variant.id, label: `${variant.product.name} — ${variant.title}`, available: (variant.inventory?.onHand || 0) - (variant.inventory?.reserved || 0) })); return <><div className="sectionHead"><div><div className="eyebrow">Sales channel</div><h2>WhatsApp</h2></div></div>{inquiries.length ? <table className="adminTable"><thead><tr><th>Contact</th><th>Phone</th><th>Product</th><th>Message</th><th>Status</th><th>Order</th><th /></tr></thead><tbody>{inquiries.map((item) => <tr key={item.id}><td>{item.customerName || "Unknown"}</td><td>{item.phone}</td><td>{item.product?.name || "—"}</td><td>{item.message || "—"}</td><td><WhatsAppInquiryStatus id={item.id} value={item.status} /></td><td>{item.convertedOrder?.orderNumber || "—"}</td><td>{item.convertedOrderId ? "Converted" : <WhatsAppConvertOrder inquiryId={item.id} variants={options} />}</td></tr>)}</tbody></table> : <div className="adminEmpty"><h3>No WhatsApp inquiries yet</h3><p>Cart and contact requests that include a phone number will appear here automatically.</p></div>}</>; }
