import {notFound} from 'next/navigation'; import CollectionEditor from '@/components/admin/collections/CollectionEditor'; import {collectionService} from '@/lib/admin/collections'; export default async function EditCollection({params}:{params:Promise<{slug:string}>}){const collection=await collectionService.get((await params).slug);if(!collection)notFound();return <CollectionEditor collection={collection}/>}
