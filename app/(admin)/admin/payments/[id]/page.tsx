import Link from'next/link';export default async function PaymentDetail({params}:{params:Promise<{id:string}>}){const{id}=await params;return <Detail title="Payment detail" label="Payment" id={id} back="/admin/payments" text="No payment provider or transaction record is connected, so payment details and refunds cannot be retrieved or changed."/>}function Detail({title,label,id,back,text}:{title:string;label:string;id:string;back:string;text:string}){return <section className="adminEditor"><div className="adminProductsIntro"><div><p className="adminEyebrow">{label}</p><h1>{title}</h1><p>Requested reference: {id}</p></div><Link href={back} className="adminSecondaryAction">Back</Link></div><div className="adminNotice"><div><strong>Record unavailable</strong><span>{text}</span></div><span className="adminNoticeTag">No persistence</span></div></section>}
