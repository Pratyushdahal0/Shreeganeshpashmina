import { rolePermissions, type AdminRole } from '@/lib/admin/auth';
const roles=Object.keys(rolePermissions) as AdminRole[];
export default function Users(){return <section className="adminUsers"><div className="adminProductsIntro"><div><p className="adminEyebrow">Security</p><h1>Users & permissions</h1><p>Server-verified identities, role-based access, and audited permission changes.</p></div></div><div className="adminNotice"><div><strong>User management is not connected</strong><span>No identity provider or user store is configured. Users, invitations, role assignments, and audit events cannot be created or changed.</span></div><span className="adminNoticeTag">Authentication pending</span></div><div className="adminPanel"><div className="adminPanelHeading"><div><p className="adminEyebrow">Role access</p><h2>Permission matrix</h2></div><span>Architecture ready</span></div><div className="adminRoleGrid">{roles.map(role=><article key={role}><strong>{role}</strong><span>{rolePermissions[role].length} granular permissions</span></article>)}</div></div><div className="adminPanel"><div className="adminPanelHeading"><div><p className="adminEyebrow">Audit trail</p><h2>Important access changes</h2></div><span>Unavailable</span></div><div className="adminEmpty"><span>○</span><p><strong>No audit events available</strong>Role and permission changes must be recorded server-side with actor, target, action, timestamp, and metadata.</p></div></div></section>}
