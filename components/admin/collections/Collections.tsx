'use client';
import Link from 'next/link';
import { useState } from 'react';
import type { Collection } from '@/lib/admin/collections';
export default function Collections({ collections }: { collections: Collection[] }) { const [query, setQuery] = useState(''); const shown = collections.filter((item) => item.name.toLowerCase().includes(query.toLowerCase())); return <section className="adminCollections"><div className="adminProductsIntro"><div><p className="adminEyebrow">Catalogue</p><h1>Collections</h1><p>Organize storefront products using the existing category source.</p></div><Link href="/admin/collections/new" className="adminPrimaryAction">Create collection</Link></div><div className="adminNotice"><div><strong>Static category source</strong><span>Existing storefront categories are shown below. Collection records, ordering, and assignments need persistence.</span></div><span className="adminNoticeTag">No persistence</span></div><div className="adminProductTools adminCollectionTools"><label className="adminSearch"><span>Search collections</span><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Collection name" /></label></div><div className="adminCollectionList">{shown.map((collection) => <article key={collection.id}><div><p className="adminEyebrow">Existing category</p><h2>{collection.name}</h2><span>Details, featured state, product assignments, ordering, and SEO are unavailable.</span></div><Link href={`/admin/collections/${collection.slug}`}>Manage</Link></article>)}</div></section>; }
