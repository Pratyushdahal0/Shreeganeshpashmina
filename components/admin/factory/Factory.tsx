'use client';import Link from'next/link';
const sections=[['Production','Production orders, stages, staff, dates, materials, and notes.','production'],['Materials','Material codes, quantities, units, suppliers, stock, and cost.','materials'],['Suppliers','Supplier contacts, materials, notes, and operating status.','suppliers'],['Quality control','Production-linked inspection results and supporting images.','quality']];
export default function Factory(){return <section className="adminFactory"><div className="adminProductsIntro"><div><p className="adminEyebrow">Manufacturing</p><h1>Factory overview</h1><p>A focused production foundation—not a full ERP.</p></div></div><div className="adminNotice"><div><strong>Factory operations are not connected</strong><span>No production, material, supplier, stock, quality-control, staff, or manufacturing statistics are available.</span></div><span className="adminNoticeTag">No persistence</span></div><section className="adminFactoryStages">{['Planned','Materials Required','In Production','Quality Check','Completed','Cancelled'].map(stage=><span key={stage}>{stage}<strong>—</strong></span>)}</section><div className="adminFactoryGrid">{sections.map(([title,text,path])=><article key={path}><p className="adminEyebrow">Factory</p><h2>{title}</h2><span>{text}</span><Link href={`/admin/factory/${path}`}>Open {title.toLowerCase()}</Link></article>)}</div></section>}
export function FactoryList({title,text}:{title:string;text:string}){return <section className="adminFactory"><div className="adminProductsIntro"><div><p className="adminEyebrow">Factory</p><h1>{title}</h1><p>{text}</p></div><Link href="/admin/factory" className="adminSecondaryAction">Back to factory</Link></div><div className="adminNotice"><div><strong>Data unavailable</strong><span>Connect the factory operations backend to manage these records. No operational data has been fabricated.</span></div><span className="adminNoticeTag">No persistence</span></div><div className="adminPanel"><div className="adminPanelHeading"><div><p className="adminEyebrow">{title}</p><h2>All records</h2></div><span>Unavailable</span></div><div className="adminEmpty"><span>○</span><p><strong>No records available</strong>{text}</p></div></div></section>}
