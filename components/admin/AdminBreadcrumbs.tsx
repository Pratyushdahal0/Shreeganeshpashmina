'use client';
import { usePathname } from 'next/navigation';

export default function AdminBreadcrumbs(){
  const path = usePathname();
  const crumb = path.startsWith('/admin/products') ? (path.endsWith('/new') ? 'Products / Create' : path === '/admin/products' ? 'Products' : 'Products / Edit') : path.startsWith('/admin/collections') ? (path.endsWith('/new') ? 'Collections / Create' : path === '/admin/collections' ? 'Collections' : 'Collections / Edit') : path.startsWith('/admin/inventory') ? 'Inventory' : path.startsWith('/admin/orders') ? (path === '/admin/orders' ? 'Orders' : 'Orders / Detail') : path.startsWith('/admin/customers') ? (path === '/admin/customers' ? 'Customers' : 'Customers / Profile') : path.startsWith('/admin/whatsapp') ? (path === '/admin/whatsapp' ? 'WhatsApp inquiries' : 'WhatsApp / Detail') : path.startsWith('/admin/wholesale') ? (path === '/admin/wholesale' ? 'Wholesale leads' : 'Wholesale / Detail') : path.startsWith('/admin/payments') ? (path === '/admin/payments' ? 'Payments' : 'Payments / Detail') : path.startsWith('/admin/shipping') ? (path === '/admin/shipping' ? 'Shipping' : 'Shipping / Detail') : path.startsWith('/admin/returns') ? (path === '/admin/returns' ? 'Returns' : 'Returns / Detail') : path.startsWith('/admin/factory') ? 'Factory' : path.startsWith('/admin/content') ? 'Content' : path.startsWith('/admin/journal') ? 'Journal' : path.startsWith('/admin/media') ? 'Media library' : path.startsWith('/admin/reviews') ? 'Reviews' : path.startsWith('/admin/discounts') ? 'Discounts' : path.startsWith('/admin/analytics') ? 'Analytics & reports' : path.startsWith('/admin/notifications') ? 'Notifications' : path.startsWith('/admin/audit-logs') ? 'Audit logs' : path.startsWith('/admin/settings') ? 'Settings & integrations' : path.startsWith('/admin/users') ? 'Users & permissions' : 'Overview';
  return <nav className="adminBreadcrumbs" aria-label="Breadcrumb">Admin / {crumb}</nav>;
}
