'use client';import {motion,AnimatePresence} from 'framer-motion';import {Icon} from './Icons';import {Product,currencies,rates} from '@/lib/data';import {whatsappUrl} from '@/lib/whatsapp';import {useEffect,useState} from 'react';
export type CartLine={product:Product;qty:number};
export default function CartDrawer({open,onClose,lines,onQty}:{open:boolean;onClose:()=>void;lines:CartLine[];onQty:(id:string,n:number)=>void}){const [currency,setCurrency]=useState<keyof typeof currencies>('USD');useEffect(()=>{const sync=()=>setCurrency((localStorage.getItem('sgp-currency') as keyof typeof currencies)||'USD');sync();addEventListener('currencychange',sync);return()=>removeEventListener('currencychange',sync)},[]);const total=lines.reduce((s,l)=>s+l.product.price*l.qty,0);const c=currencies[currency];const money=(v:number)=>`${c.symbol}${Math.round(v*rates[currency]).toLocaleString()}`;const msg=`Hello Shree Ganesh Pashmina, I would like to place an order.\n\n${lines.map(l=>`• ${l.product.name} × ${l.qty} — ${money(l.product.price*l.qty)}`).join('\n')}\n\nEstimated total: ${money(total)}\nCurrency: ${currency}`;return <AnimatePresence>{open&&<><motion.div className="drawerBackdrop" initial={{opacity:0}} animate={{opacity:1}} exit={{opacity:0}} onClick={onClose}/><motion.aside className="drawer" initial={{x:'100%'}} animate={{x:0}} exit={{x:'100%'}} transition={{duration:.6,ease:[.16,1,.3,1]}}><div className="drawerHeader"><span className="eyebrow">Your bag · {lines.length}</span><button className="iconBtn" onClick={onClose}><Icon name="x"/></button></div><div className="drawerBody">{lines.length===0?<div style={{paddingTop:60,textAlign:'center'}}><p className="serif" style={{fontSize:32}}>Your bag is quiet.</p><p className="muted">Add a piece and continue through WhatsApp.</p></div>:lines.map(l=><div className="cartItem" key={l.product.id}><img src={l.product.image} alt=""/><div><div className="productName">{l.product.name}</div><div className="productMaterial">{l.product.material}</div><div className="qty"><button onClick={()=>onQty(l.product.id,l.qty-1)}><Icon name="minus" size="xs"/></button><span>{l.qty}</span><button onClick={()=>onQty(l.product.id,l.qty+1)}><Icon name="plus" size="xs"/></button></div></div><div style={{fontSize:13}}>{money(l.product.price*l.qty)}</div></div>)}</div>{lines.length>0&&<div className="drawerFooter"><div className="shippingProgress">{total < 400 ? <><div className="shippingProgressTop"><span>Worldwide delivery</span><strong>{money(400-total)} away</strong></div><div className="shippingTrack"><span style={{width:`${Math.min((total/400)*100,100)}%`}} /></div><p>Add {money(400-total)} more for complimentary worldwide delivery.</p></> : <><div className="shippingUnlocked"><Icon name="check" size="xs"/><span>Complimentary worldwide delivery unlocked.</span></div></>}</div><div className="total"><span>Estimated total</span><strong>{money(total)}</strong></div><a className="btn dark" style={{justifyContent:'center',width:'100%'}} href={whatsappUrl(msg)} target="_blank" rel="noreferrer">Continue via WhatsApp <Icon name="whatsapp"/></a><p className="muted" style={{fontSize:10,lineHeight:1.6,marginTop:12}}>Final shipping cost and payment instructions will be confirmed in WhatsApp. Online payment is not enabled in V1.</p></div>}</motion.aside></>}</AnimatePresence>}
