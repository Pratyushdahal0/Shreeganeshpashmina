export type DiscountType='percentage'|'fixed';export type Discount={id:string;code:string|null;type:DiscountType;value:number;productIds:string[];collectionIds:string[];minimumOrderAmount:number|null;startsAt:string|null;endsAt:string|null;usageLimit:number|null;customerIds:string[];active:boolean};export type DiscountMutationResult={ok:false;reason:'persistence_unavailable';message:string};export type DiscountValidation={valid:boolean;errors:string[]};
/** Run this server-side in the eventual promotion backend before every save/redemption. */ export const validateDiscount=(discount:Partial<Discount>):DiscountValidation=>{const errors:string[]=[];if(!discount.type)errors.push('Discount type is required.');if(discount.value===undefined||discount.value<=0)errors.push('Discount value must be greater than zero.');if(discount.type==='percentage'&&discount.value!==undefined&&discount.value>100)errors.push('Percentage discounts cannot exceed 100%.');if(discount.type==='fixed'&&discount.value!==undefined&&discount.minimumOrderAmount!==null&&discount.minimumOrderAmount!==undefined&&discount.value>discount.minimumOrderAmount)errors.push('A fixed discount cannot exceed its minimum order amount.');if(discount.startsAt&&discount.endsAt&&new Date(discount.endsAt)<new Date(discount.startsAt))errors.push('End date must be after start date.');if(discount.usageLimit!==null&&discount.usageLimit!==undefined&&discount.usageLimit<1)errors.push('Usage limit must be at least one.');return{valid:errors.length===0,errors}};
export interface DiscountService{list(query?:string):Promise<{state:'unavailable'|'live';discounts:Discount[]}>;get(id:string):Promise<Discount|null>;save(discount:Partial<Discount>):Promise<DiscountMutationResult>;archive(id:string):Promise<DiscountMutationResult>;}
const unavailable=():DiscountMutationResult=>({ok:false,reason:'persistence_unavailable',message:'Discount data is not connected; no promotion was created, changed, or archived.'});export const discountService:DiscountService={async list(){return{state:'unavailable',discounts:[]}},async get(){return null},async save(){return unavailable()},async archive(){return unavailable()}};
